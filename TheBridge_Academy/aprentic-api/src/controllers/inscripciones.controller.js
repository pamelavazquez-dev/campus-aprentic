const inscripcionesService = require('../services/inscripciones.service');

// TODO: getAll(req, res, next): obtener todas las inscripciones
// TODO: getById(req, res, next): obtener inscripcion por ID
// TODO: create(req, res, next): crear nueva inscripcion
// TODO: update(req, res, next): actualizar inscripcion existente
// TODO: delete(req, res, next): eliminar inscripcion
// TODO: getByAlumno(req, res, next): obtener inscripciones por alumno
// TODO: getByCurso(req, res, next): obtener inscripciones por curso

module.exports = {
  // Controllers aqui
};
