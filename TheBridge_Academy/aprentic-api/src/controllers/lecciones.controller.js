const leccionesService = require('../services/lecciones.service');

// TODO: getAll(req, res, next): obtener todas las lecciones
// TODO: getById(req, res, next): obtener leccion por ID
// TODO: create(req, res, next): crear nueva leccion
// TODO: update(req, res, next): actualizar leccion existente
// TODO: delete(req, res, next): eliminar leccion
// TODO: getByModulo(req, res, next): obtener lecciones por modulo

module.exports = {
  // Controllers aqui
};
