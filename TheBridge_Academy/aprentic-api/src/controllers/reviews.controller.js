const reviewsService = require('../services/reviews.service');

// TODO: getAll(req, res, next): obtener todas las resenas
// TODO: getById(req, res, next): obtener resena por ID
// TODO: create(req, res, next): crear nueva resena
// TODO: update(req, res, next): actualizar resena existente
// TODO: delete(req, res, next): eliminar resena
// TODO: getByCurso(req, res, next): obtener resenas por curso
// TODO: getByAlumno(req, res, next): obtener resenas por alumno

module.exports = {
  // Controllers aqui
};
