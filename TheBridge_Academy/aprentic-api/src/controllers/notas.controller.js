const notasService = require('../services/notas.service');

// TODO: getAll(req, res, next): obtener todas las notas
// TODO: getById(req, res, next): obtener nota por ID
// TODO: create(req, res, next): crear nueva nota
// TODO: update(req, res, next): actualizar nota existente
// TODO: delete(req, res, next): eliminar nota
// TODO: getByProyecto(req, res, next): obtener notas por proyecto
// TODO: getByAlumno(req, res, next): obtener notas por alumno
// TODO: getByProfesor(req, res, next): obtener notas por profesor

module.exports = {
  // Controllers aqui
};
