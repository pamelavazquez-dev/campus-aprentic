const usuariosService = require('../services/usuarios.service');

// TODO: getAll(req, res, next): obtener todos los usuarios
// TODO: getById(req, res, next): obtener usuario por ID
// TODO: create(req, res, next): crear nuevo usuario
// TODO: update(req, res, next): actualizar usuario existente
// TODO: delete(req, res, next): eliminar usuario
// TODO: getByEmail(req, res, next): obtener usuario por email
// TODO: getByRol(req, res, next): obtener usuarios por rol

module.exports = {
  // Controllers aqui
};
