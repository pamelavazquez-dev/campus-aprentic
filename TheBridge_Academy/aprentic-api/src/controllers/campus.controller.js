const campusService = require('../services/campus.service');

// TODO: getAll(req, res, next): obtener todos los campus
// TODO: getById(req, res, next): obtener campus por ID
// TODO: create(req, res, next): crear nuevo campus
// TODO: update(req, res, next): actualizar campus existente
// TODO: delete(req, res, next): eliminar campus

module.exports = {
  // Controllers aqui
};
