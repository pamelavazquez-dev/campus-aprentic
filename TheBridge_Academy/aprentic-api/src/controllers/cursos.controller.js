const cursosService = require('../services/cursos.service');

// TODO: getAll(req, res, next): obtener todos los cursos
// TODO: getById(req, res, next): obtener curso por ID
// TODO: create(req, res, next): crear nuevo curso
// TODO: update(req, res, next): actualizar curso existente
// TODO: delete(req, res, next): eliminar curso
// TODO: getByCampus(req, res, next): obtener cursos por campus
// TODO: getByInstructor(req, res, next): obtener cursos por instructor

module.exports = {
  // Controllers aqui
};
