const express = require('express');
const leccionesController = require('../controllers/lecciones.controller');

const router = express.Router();

// TODO: GET /api/lecciones - obtener todas las lecciones
// TODO: GET /api/lecciones/:id - obtener leccion por ID
// TODO: POST /api/lecciones - crear nueva leccion
// TODO: PUT /api/lecciones/:id - actualizar leccion existente
// TODO: DELETE /api/lecciones/:id - eliminar leccion
// TODO: GET /api/lecciones/modulo/:moduloId - obtener lecciones por modulo

module.exports = router;
