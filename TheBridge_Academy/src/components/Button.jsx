import React from 'react'

export default function Button({ children }) {
  return (
    <button>
      {/* TODO: Crear componente Button con variantes primary, secondary y ghost, tamaños sm/md/lg y estados default/hover/disabled. */}
      {children}
    </button>
  )
}
