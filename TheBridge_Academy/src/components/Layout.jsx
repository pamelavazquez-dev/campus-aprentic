import React from 'react'

export default function Layout({ children }) {
  return (
    <div>
      {/* TODO: Wrap pages with shared layout, header, footer, and global UI state here. */}
      {children}
    </div>
  )
}
