import React from 'react'

export default function Card({ children }) {
  return (
    <article>
      {/* TODO: Crear componente Card reutilizable para course, metric y lesson con elevation, interactive y media. */}
      {children}
    </article>
  )
}
